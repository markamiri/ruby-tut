<h1>All Logos</h1>

<%= link_to "Upload New Logo", new_logo_path, class: "sb-btn sb-btn--success" %>

<ul>
  <% @logos.each do |logo| %>
    <li>
      <%= link_to image_tag(logo.image, style: "height: 80px;") if logo.image.attached?, logo %>
    </li>
  <% end %>
</ul>