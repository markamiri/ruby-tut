<tr id="<%= dom_id category %>">
  <td><%= category.name %></td>
  <td><%= category.slug %></td>
  <td><%= category.order %></td>
  <td><%= category.products_count %></td>

  <td>
    <%= link_to "Edit", category, class: "sb-btn sb-btn--sm" %>
  </td>
</tr>
